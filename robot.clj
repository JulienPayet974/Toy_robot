; Fonction pour placer le robot sur la table
(defn place [x y direction])


; Fonction pour déplacer le robot 
(defn move [robot])
; Fonction pour faire tourner le robot 
(defn rotate [robot direction])

; Fonction pour afficher la position et la direction du robot
(defn report [robot])

; Fonction pour exécuter une commande 
(defn run [robot command])